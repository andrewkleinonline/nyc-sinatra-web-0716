<table>
  <tr>
    <th>Name</th>
    <th>Landmarks</th>
  </tr>
  <tr>
    <td><%= @figure.name %></td>
    <td>
      <ul>
        <% @figure.landmarks.each do |landmark| %>
          <li><%= landmark.name %></li>
        <% end %>
      </ul>
    </td>
  </tr>
<table>

<a href="/figures"><input type="button" name="name" value="Back to list of Figures"></a>
<a href="/figures/<%=  @figure.id %>/edit"><input type="button" name="name" value="Edit this Figure"></a>
